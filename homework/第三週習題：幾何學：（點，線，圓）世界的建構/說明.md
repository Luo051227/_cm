# 📐 I. 幾何物件的定義
## 1. 點 (Point)
點是零維的幾何實體，通常用其在 $n$ 維空間中的坐標來表示。在平面幾何中，點是一個有序數對。

`數學表示： P = (x, y)`
## 2. 直線 (Line)
直線是一維的、無限延伸的、由無數點組成的集合。

一般式 (Standard Form)：` Ax + By + C = 0，其中 $A$ 和 $B$ 不全為零。`
斜截式 (Slope-intercept Form)：`y = mx + b，其中 $m$ 是斜率，$b$ 是 $y$ 軸截距。
